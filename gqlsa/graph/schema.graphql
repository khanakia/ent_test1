directive @canAdmin on FIELD_DEFINITION


scalar Time
scalar Uint64
scalar Map
scalar JSON


type Student {
  name: String!
}

extend type Query {
  ping: String!
}

extend type Mutation {
  ping: String!
}
