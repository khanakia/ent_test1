directive @canAdmin on FIELD_DEFINITION
directive @canApp on FIELD_DEFINITION
# directive @appGql on FIELD_DEFINITION

scalar Time
scalar Uint
scalar Uint64
scalar Map
scalar JSON
scalar JsonSlice

type Student {
  name: String!
}

extend type Query {
  ping: String!
}

extend type Mutation {
  ping: String!
}
