// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"saas/gen/ent/keyvalue"
	"saas/gen/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// KeyvalueQuery is the builder for querying Keyvalue entities.
type KeyvalueQuery struct {
	config
	ctx        *QueryContext
	order      []keyvalue.OrderOption
	inters     []Interceptor
	predicates []predicate.Keyvalue
	loadTotal  []func(context.Context, []*Keyvalue) error
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the KeyvalueQuery builder.
func (kq *KeyvalueQuery) Where(ps ...predicate.Keyvalue) *KeyvalueQuery {
	kq.predicates = append(kq.predicates, ps...)
	return kq
}

// Limit the number of records to be returned by this query.
func (kq *KeyvalueQuery) Limit(limit int) *KeyvalueQuery {
	kq.ctx.Limit = &limit
	return kq
}

// Offset to start from.
func (kq *KeyvalueQuery) Offset(offset int) *KeyvalueQuery {
	kq.ctx.Offset = &offset
	return kq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (kq *KeyvalueQuery) Unique(unique bool) *KeyvalueQuery {
	kq.ctx.Unique = &unique
	return kq
}

// Order specifies how the records should be ordered.
func (kq *KeyvalueQuery) Order(o ...keyvalue.OrderOption) *KeyvalueQuery {
	kq.order = append(kq.order, o...)
	return kq
}

// First returns the first Keyvalue entity from the query.
// Returns a *NotFoundError when no Keyvalue was found.
func (kq *KeyvalueQuery) First(ctx context.Context) (*Keyvalue, error) {
	nodes, err := kq.Limit(1).All(setContextOp(ctx, kq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{keyvalue.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (kq *KeyvalueQuery) FirstX(ctx context.Context) *Keyvalue {
	node, err := kq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Keyvalue ID from the query.
// Returns a *NotFoundError when no Keyvalue ID was found.
func (kq *KeyvalueQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = kq.Limit(1).IDs(setContextOp(ctx, kq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{keyvalue.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (kq *KeyvalueQuery) FirstIDX(ctx context.Context) string {
	id, err := kq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Keyvalue entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Keyvalue entity is found.
// Returns a *NotFoundError when no Keyvalue entities are found.
func (kq *KeyvalueQuery) Only(ctx context.Context) (*Keyvalue, error) {
	nodes, err := kq.Limit(2).All(setContextOp(ctx, kq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{keyvalue.Label}
	default:
		return nil, &NotSingularError{keyvalue.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (kq *KeyvalueQuery) OnlyX(ctx context.Context) *Keyvalue {
	node, err := kq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Keyvalue ID in the query.
// Returns a *NotSingularError when more than one Keyvalue ID is found.
// Returns a *NotFoundError when no entities are found.
func (kq *KeyvalueQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = kq.Limit(2).IDs(setContextOp(ctx, kq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{keyvalue.Label}
	default:
		err = &NotSingularError{keyvalue.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (kq *KeyvalueQuery) OnlyIDX(ctx context.Context) string {
	id, err := kq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Keyvalues.
func (kq *KeyvalueQuery) All(ctx context.Context) ([]*Keyvalue, error) {
	ctx = setContextOp(ctx, kq.ctx, ent.OpQueryAll)
	if err := kq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Keyvalue, *KeyvalueQuery]()
	return withInterceptors[[]*Keyvalue](ctx, kq, qr, kq.inters)
}

// AllX is like All, but panics if an error occurs.
func (kq *KeyvalueQuery) AllX(ctx context.Context) []*Keyvalue {
	nodes, err := kq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Keyvalue IDs.
func (kq *KeyvalueQuery) IDs(ctx context.Context) (ids []string, err error) {
	if kq.ctx.Unique == nil && kq.path != nil {
		kq.Unique(true)
	}
	ctx = setContextOp(ctx, kq.ctx, ent.OpQueryIDs)
	if err = kq.Select(keyvalue.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (kq *KeyvalueQuery) IDsX(ctx context.Context) []string {
	ids, err := kq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (kq *KeyvalueQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, kq.ctx, ent.OpQueryCount)
	if err := kq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, kq, querierCount[*KeyvalueQuery](), kq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (kq *KeyvalueQuery) CountX(ctx context.Context) int {
	count, err := kq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (kq *KeyvalueQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, kq.ctx, ent.OpQueryExist)
	switch _, err := kq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (kq *KeyvalueQuery) ExistX(ctx context.Context) bool {
	exist, err := kq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the KeyvalueQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (kq *KeyvalueQuery) Clone() *KeyvalueQuery {
	if kq == nil {
		return nil
	}
	return &KeyvalueQuery{
		config:     kq.config,
		ctx:        kq.ctx.Clone(),
		order:      append([]keyvalue.OrderOption{}, kq.order...),
		inters:     append([]Interceptor{}, kq.inters...),
		predicates: append([]predicate.Keyvalue{}, kq.predicates...),
		// clone intermediate query.
		sql:       kq.sql.Clone(),
		path:      kq.path,
		modifiers: append([]func(*sql.Selector){}, kq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Keyvalue.Query().
//		GroupBy(keyvalue.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (kq *KeyvalueQuery) GroupBy(field string, fields ...string) *KeyvalueGroupBy {
	kq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &KeyvalueGroupBy{build: kq}
	grbuild.flds = &kq.ctx.Fields
	grbuild.label = keyvalue.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Keyvalue.Query().
//		Select(keyvalue.FieldCreatedAt).
//		Scan(ctx, &v)
func (kq *KeyvalueQuery) Select(fields ...string) *KeyvalueSelect {
	kq.ctx.Fields = append(kq.ctx.Fields, fields...)
	sbuild := &KeyvalueSelect{KeyvalueQuery: kq}
	sbuild.label = keyvalue.Label
	sbuild.flds, sbuild.scan = &kq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a KeyvalueSelect configured with the given aggregations.
func (kq *KeyvalueQuery) Aggregate(fns ...AggregateFunc) *KeyvalueSelect {
	return kq.Select().Aggregate(fns...)
}

func (kq *KeyvalueQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range kq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, kq); err != nil {
				return err
			}
		}
	}
	for _, f := range kq.ctx.Fields {
		if !keyvalue.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if kq.path != nil {
		prev, err := kq.path(ctx)
		if err != nil {
			return err
		}
		kq.sql = prev
	}
	return nil
}

func (kq *KeyvalueQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Keyvalue, error) {
	var (
		nodes = []*Keyvalue{}
		_spec = kq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Keyvalue).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Keyvalue{config: kq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(kq.modifiers) > 0 {
		_spec.Modifiers = kq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, kq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	for i := range kq.loadTotal {
		if err := kq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (kq *KeyvalueQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := kq.querySpec()
	if len(kq.modifiers) > 0 {
		_spec.Modifiers = kq.modifiers
	}
	_spec.Node.Columns = kq.ctx.Fields
	if len(kq.ctx.Fields) > 0 {
		_spec.Unique = kq.ctx.Unique != nil && *kq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, kq.driver, _spec)
}

func (kq *KeyvalueQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(keyvalue.Table, keyvalue.Columns, sqlgraph.NewFieldSpec(keyvalue.FieldID, field.TypeString))
	_spec.From = kq.sql
	if unique := kq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if kq.path != nil {
		_spec.Unique = true
	}
	if fields := kq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, keyvalue.FieldID)
		for i := range fields {
			if fields[i] != keyvalue.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := kq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := kq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := kq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := kq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (kq *KeyvalueQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(kq.driver.Dialect())
	t1 := builder.Table(keyvalue.Table)
	columns := kq.ctx.Fields
	if len(columns) == 0 {
		columns = keyvalue.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if kq.sql != nil {
		selector = kq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if kq.ctx.Unique != nil && *kq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range kq.modifiers {
		m(selector)
	}
	for _, p := range kq.predicates {
		p(selector)
	}
	for _, p := range kq.order {
		p(selector)
	}
	if offset := kq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := kq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (kq *KeyvalueQuery) Modify(modifiers ...func(s *sql.Selector)) *KeyvalueSelect {
	kq.modifiers = append(kq.modifiers, modifiers...)
	return kq.Select()
}

// KeyvalueGroupBy is the group-by builder for Keyvalue entities.
type KeyvalueGroupBy struct {
	selector
	build *KeyvalueQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (kgb *KeyvalueGroupBy) Aggregate(fns ...AggregateFunc) *KeyvalueGroupBy {
	kgb.fns = append(kgb.fns, fns...)
	return kgb
}

// Scan applies the selector query and scans the result into the given value.
func (kgb *KeyvalueGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, kgb.build.ctx, ent.OpQueryGroupBy)
	if err := kgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KeyvalueQuery, *KeyvalueGroupBy](ctx, kgb.build, kgb, kgb.build.inters, v)
}

func (kgb *KeyvalueGroupBy) sqlScan(ctx context.Context, root *KeyvalueQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(kgb.fns))
	for _, fn := range kgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*kgb.flds)+len(kgb.fns))
		for _, f := range *kgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*kgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := kgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// KeyvalueSelect is the builder for selecting fields of Keyvalue entities.
type KeyvalueSelect struct {
	*KeyvalueQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ks *KeyvalueSelect) Aggregate(fns ...AggregateFunc) *KeyvalueSelect {
	ks.fns = append(ks.fns, fns...)
	return ks
}

// Scan applies the selector query and scans the result into the given value.
func (ks *KeyvalueSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ks.ctx, ent.OpQuerySelect)
	if err := ks.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KeyvalueQuery, *KeyvalueSelect](ctx, ks.KeyvalueQuery, ks, ks.inters, v)
}

func (ks *KeyvalueSelect) sqlScan(ctx context.Context, root *KeyvalueQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ks.fns))
	for _, fn := range ks.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ks.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ks.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ks *KeyvalueSelect) Modify(modifiers ...func(s *sql.Selector)) *KeyvalueSelect {
	ks.modifiers = append(ks.modifiers, modifiers...)
	return ks
}
